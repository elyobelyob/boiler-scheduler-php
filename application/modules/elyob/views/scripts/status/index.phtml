<?php echo $this->partial('partials/time.phtml'); ?>

<?php $this->headScript()->appendFile('/js/jquery-ui-timepicker-addon.js');?>

<?php
$filename = "/var/log/boiler/connectivity.tmp";
$handle = fopen($filename, "r");
$contents = fread($handle, filesize($filename));
fclose($handle);

echo nl2br($contents);

echo "</br>";

$filename = "/var/log/boiler/boiler_scheduler.tmp";
$handle = fopen($filename, "r");
$contents = fread($handle, filesize($filename));
fclose($handle);

echo nl2br($contents);

echo "</br>";

$filename = "/var/log/boiler/system_check.tmp";
$handle = fopen($filename, "r");
$contents = fread($handle, filesize($filename));
fclose($handle);

echo nl2br($contents);

/*
$filename = "http://192.168.1.70/boiler/system-check.tmp";
$news = fopen($filename, "r") or die ("Link does not exist!");
unset($text);
while (!feof($news)) {
  $newbuffer = fgets($news, 4096);
  $text[] = $newbuffer;
};
fclose($news);
$text = implode(" ", $text);

echo nl2br($text);
*/
